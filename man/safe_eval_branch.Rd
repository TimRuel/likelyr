% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/branch-mode-utils.R, R/branch-utils.R
\name{safe_eval_branch}
\alias{safe_eval_branch}
\title{Safely Evaluate Branch Log-Likelihood}
\usage{
safe_eval_branch(psi, param_init, branch_evaluator)

safe_eval_branch(psi, param_init, branch_evaluator)
}
\arguments{
\item{psi}{Numeric scalar ψ value.}

\item{param_init}{Numeric vector initial θ guess.}

\item{branch_evaluator}{Function(psi, param_init) → list(param_hat, branch_val).}

\item{branch_fn}{Function(psi, param_init) → list(param_hat, branch_val).}
}
\value{
Numeric scalar log-likelihood value (possibly \code{-Inf}).

Numeric scalar log-likelihood value (possibly \code{-Inf}).
}
\description{
Evaluates the branch log-likelihood at a given ψ value using
\code{branch_fn}, returning \code{-Inf} on failure.

This is intended for coarse scans and bracketing, where robustness
is more important than propagating solver errors.

Evaluates the branch log-likelihood at a given ψ value using
\code{branch_evaluator}, returning \code{-Inf} on failure.

This is intended for coarse scans and bracketing, where robustness
is more important than propagating solver errors.
}
\keyword{internal}
