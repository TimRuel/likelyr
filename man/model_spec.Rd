% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-model.R
\name{model_spec}
\alias{model_spec}
\title{Create a Model Specification}
\usage{
model_spec(
  parameter = NULL,
  likelihood = NULL,
  estimand = NULL,
  pipeline = NULL,
  solver = NULL,
  execution = NULL,
  name = NULL,
  ...
)
}
\description{
A model specification declares the components needed for
likelihood-based inference.

All components are required \emph{prior to calibration}:
• parameter_spec()
• likelihood_spec()
• estimand_spec()
• pipeline_spec()
• solver_spec()
• execution_spec()

However, \code{model_spec()} is intentionally permissive at construction:
you may initialize with \code{NULL} components and add them incrementally
via \code{\link[=add]{add()}}.

After calibration:
\itemize{
\item Structural specifications (parameter, likelihood, estimand) are frozen.
\item Numerical and procedural specifications (pipeline, solver, execution)
may be updated to support alternative inference or execution
behavior without recalibration.
}
}
