% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/branch-traverse.R
\name{traverse_branch_topdown}
\alias{traverse_branch_topdown}
\title{Traverse a Branch Outward from a Pre-Located Mode}
\usage{
traverse_branch_topdown(seed, pipeline, grid, branch_evaluator, effective_crit)
}
\arguments{
\item{seed}{A named list containing at minimum:
\itemize{
\item \code{omega_hat}      — numeric vector
\item \code{psi_hat}        — numeric scalar (topdown only)
\item \code{param_hat}      — numeric vector (topdown only)
\item \code{loglik_at_mode} — numeric scalar (topdown only)
}}

\item{pipeline}{A calibrated \code{pipeline_spec} object.}

\item{grid}{A ψ-grid object from \code{psi_grid_anchor()}.}

\item{branch_evaluator}{Function \code{(psi, param_init) -> list(param_hat, branch_val)}.}

\item{effective_crit}{Numeric scalar. Log-likelihood drop defining the
branch cutoff: \code{cutoff = loglik_at_mode - effective_crit}.}
}
\description{
Starting from a mode already located by \code{screen()}, sweeps
outward in both directions along the ψ-grid, stopping when the
log-likelihood drops below \code{loglik_at_mode - effective_crit}
or a ψ bound is reached.

This is the primary traversal strategy. It requires a pre-populated
\code{seed} containing \code{psi_hat}, \code{param_hat}, and
\code{loglik_at_mode} — all of which are stored by \code{screen()}.
}
\keyword{internal}
