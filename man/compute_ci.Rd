% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ci.R, R/utils-ll-processing.R
\name{compute_ci}
\alias{compute_ci}
\title{Compute Likelihood-Ratio Confidence Intervals (single curve)}
\usage{
compute_ci(relative_loglik_fn, df, MLE_data, alpha_levels)

compute_ci(relative_loglik_fn, df, MLE_data, alpha_levels)
}
\arguments{
\item{relative_loglik_fn}{A function rLL(psi).}

\item{df}{A data frame with columns psi and loglik (grid).}

\item{MLE_data}{Output from compute_MLE().}

\item{alpha_levels}{Numeric vector of α levels.}
}
\value{
A tibble with (confidence, alpha, lower, upper).

A tibble with (confidence, alpha, lower, upper).
}
\description{
Given a relative log-likelihood function:
rLL(psi) = loglik(psi) - max_loglik

compute LR-based confidence intervals satisfying:
rLL(psi) >= -crit
where crit = 0.5 * χ²₁(1 - α).

Given a relative log-likelihood function:
rLL(psi) = loglik(psi) - max_loglik

compute LR-based confidence intervals satisfying:
rLL(psi) >= -crit
where crit = 0.5 * χ²₁(1 - α).
}
