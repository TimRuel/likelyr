% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/branch-mode.R
\name{branch_mode_solve}
\alias{branch_mode_solve}
\title{Solve for the Continuous Branch Mode ψ̂ (Internal)}
\usage{
branch_mode_solve(
  psi_mle,
  eval_psi_fun,
  param_init,
  search_interval,
  retries = 3,
  expand_factor = 1.5
)
}
\arguments{
\item{psi_mle}{Numeric scalar. Global ψ MLE (used as the initial Brent point).}

\item{eval_psi_fun}{Function with signature \code{f(psi, param_init)} returning
a list \code{(param_hat, branch_val)} for that ψ.}

\item{param_init}{Initial nuisance parameter vector for inner optimizations.}

\item{search_interval}{Numeric vector of length 2 giving lower/upper bounds for ψ.}

\item{retries}{Integer. Number of boundary-expansion attempts if Brent mode
lands on a boundary. Default: 3.}

\item{expand_factor}{Numeric expansion multiplier when increasing interval
width around \code{psi_mle}. Default: 1.5.}
}
\value{
A list with components:
\describe{
\item{psi_hat}{Continuous maximizer (branch-specific mode).}
\item{param_hat}{Nuisance optimizer θ̂ evaluated at \code{psi_hat}.}
\item{loglik_at_mode}{Log-likelihood at the mode.}
}
}
\description{
Finds the \strong{branch-specific mode} \eqn{\hat{\psi}} by maximizing the
branch log-likelihood over a \emph{continuous} ψ domain.

The evaluation function \code{eval_psi_fun(psi, param_init)} returns:
\itemize{
\item \code{param_hat} — the nuisance-optimized θ̂(ψ)
\item \code{branch_val} — log-likelihood at θ̂(ψ)
}

This routine then solves:
\deqn{
  \hat{\psi} = \arg\max_{\psi \in \mathcal{I}} \; \ell(\hat{\param}(\psi))
}

using a Brent optimization over the user-defined interval.
The resulting mode \strong{is not aligned to the ψ-grid}.
Grid-snapping happens later inside \code{get_adjacent_psi_points()}.
}
\details{
If the Brent solution lands exactly on the boundary of the allowed search
interval, the function expands the interval \emph{around ψ_MLE} and retries up to
\code{retries} times. This prevents misleading solutions caused by an overly
narrow initial range.
}
\keyword{internal}
