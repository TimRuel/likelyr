% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/branch-adjacent.R
\name{get_adjacent_psi_points}
\alias{get_adjacent_psi_points}
\title{Compute nearest ψ grid points to a branch-specific mode}
\usage{
get_adjacent_psi_points(psi_hat_branch, psi_mle, increment)
}
\arguments{
\item{psi_hat_branch}{Numeric scalar. The ψ value at which the branch
log-likelihood attains its maximum (branch-specific mode).}

\item{psi_mle}{Numeric scalar. The global ψ MLE (acts as grid origin).}

\item{increment}{Positive numeric scalar. The grid spacing between ψ values.}
}
\value{
A named list with components:
\describe{
\item{left}{Grid point immediately ≤ \code{psi_hat_branch}.}
\item{right}{Grid point immediately ≥ \code{psi_hat_branch}.}
\item{k_left}{Integer index of the left grid point relative to \code{psi_MLE}.}
\item{k_right}{Integer index of the right grid point relative to \code{psi_MLE}.}
}
}
\description{
Internal helper that identifies the immediate left and right ψ grid points
surrounding a branch-specific ψ mode (\code{psi_hat_branch}), assuming a uniform
grid of the form:

\deqn{\psi_k = \psi_{\text{MLE}} + k \cdot \text{increment}}

where \code{k} is an integer grid index.

This function does \strong{not} construct the full grid. It computes the
nearest grid-aligned values algebraically, which is fast and avoids
unnecessary vector generation when extending branch evaluations.
}
\examples{
get_adjacent_psi_points(psi_hat_branch = 1.13, psi_MLE = 0, increment = 0.25)

get_adjacent_psi_points(psi_hat_branch = -0.68, psi_MLE = 0, increment = 0.25)
}
\keyword{internal}
