% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate}
\alias{calibrate}
\title{Calibrate a likelihood workflow to observed data}
\usage{
calibrate(wf, data, ...)
}
\arguments{
\item{wf}{A fully specified \code{likelihood_workflow} object.}

\item{data}{Observed data structure in any form required by the user’s
likelihood and estimand implementations.}

\item{...}{Additional arguments forwarded to the optimizer if relevant.}
}
\value{
An S3 object of class \code{likelihood_calibration} containing:
\itemize{
\item \code{workflow}      Original workflow object
\item \code{data}          Stored data for reference
\item \code{theta_mle}     MLE (supplied or computed)
\item \code{psi_mle}       Estimand value at MLE
\item \code{loglik}        Data-bound function(theta)
\item \code{E_loglik}      Data+omega-bound function(theta, omega_hat)
\item \code{psi}           Data-bound estimand function(theta)
}
}
\description{
\code{calibrate()} takes a fully specified \code{likelihood_workflow} and observed
data, embeds the data into the model and estimand (if requested),
determines the full-model MLE \code{theta_mle}, and evaluates the estimand
\code{psi_mle}. This produces a calibration object which is the required
starting point for branch-based profile or integrated likelihoods.

Unlike the workflow itself, which is purely declarative and data-free,
the calibration output \emph{binds} data to the likelihood and estimand so
they become functions of the model parameters alone.
}
