% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omega-hat-strategies.R
\name{omega_hat_initgen_gaussian}
\alias{omega_hat_initgen_gaussian}
\title{Omega-Hat Initial-Guess Generator: Gaussian Strategy}
\usage{
omega_hat_initgen_gaussian(
  param_mle,
  param_dim,
  param_lower = NULL,
  param_upper = NULL,
  psi_jac = NULL,
  local_scale = 0.15,
  global_scale = 0.6,
  p_local = 0.7,
  return_intent = TRUE,
  p_recenter = 0.1,
  p_far_recenter = 0.75,
  ...
)
}
\arguments{
\item{param_mle}{Numeric vector. MLE of the full parameter.}

\item{param_dim}{Integer. Length of param_mle.}

\item{param_lower}{Optional numeric vector. Lower bounds.}

\item{param_upper}{Optional numeric vector. Upper bounds.}

\item{psi_jac}{Optional function(param) → gradient ∇ψ(θ).}

\item{local_scale}{Positive scalar. SD for local perturbations.}

\item{global_scale}{Positive scalar. SD for global perturbations.}

\item{p_local}{Numeric in (0,1). Probability of local vs global scale.}

\item{return_intent}{Logical. If TRUE, returns a named list with
\code{x0}, \code{dir}, and \code{move_type} (intent object).
If FALSE, returns a plain numeric vector.}

\item{p_recenter}{Numeric in [0,1). Probability of recentering around
a history point rather than param_mle.}

\item{p_far_recenter}{Numeric in (0,1]. When recentering, probability of
choosing the farthest history point (vs a random one).}

\item{...}{Unused. Absorbed for forward compatibility.}
}
\value{
A closure \code{function(history = NULL)} returning either an
intent list or a numeric vector, depending on \code{return_intent}.
}
\description{
Generates initial guesses for omega-hat by perturbing \code{param_mle}
in the tangent space of the ψ constraint surface. When a Jacobian is
available, perturbations are restricted to the subspace orthogonal to
∇ψ, providing first-order feasibility. Otherwise, isotropic Gaussian
jitter is applied directly.

A mixture of local and global scales balances stability and exploration.
Optional recentering around previously accepted omega-hats (via
\code{history}) biases toward unexplored regions when \code{p_far_recenter}
is high.
}
