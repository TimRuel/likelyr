% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-traversal.R
\name{traversal_spec}
\alias{traversal_spec}
\title{Specify the Branch Traversal Strategy}
\usage{
traversal_spec(
  increment,
  traversal_method = "topdown",
  mode_locator_fn = NULL,
  confidence_levels = c(0.9, 0.95, 0.99),
  cutoff_buffer = 1.5,
  n_adjacent = 3L,
  max_mode_shifts = 20L,
  k_recent = 3L,
  drop_multiplier = 5,
  max_trials = NULL,
  name = NULL,
  ...
)
}
\arguments{
\item{increment}{Required. Positive numeric scalar, ψ-grid spacing.}

\item{traversal_method}{Character scalar. One of \code{"topdown"},
\code{"leftright"}. Default: \code{"topdown"}.}

\item{mode_locator_fn}{Optional constructor function returning a
closure \code{function(omega_hat) -> mode result}. When \code{NULL},
the built-in \code{bracket_gss} locator is used.}

\item{confidence_levels}{Numeric vector in (0, 1).
Default: \code{c(0.90, 0.95, 0.99)}.}

\item{cutoff_buffer}{Positive numeric scalar. Each branch is required
to extend this multiple of the theoretical minimum cutoff distance.
Default: \code{1.5}.}

\item{n_adjacent}{Non-negative integer. Number of grid points to
evaluate on each side of the mode during \code{probe()}.
Default: \code{3L}.}

\item{max_mode_shifts}{Non-negative integer. Maximum number of mode
shifts permitted during \code{probe()} before rejecting.
Default: \code{20L}.}

\item{k_recent}{Non-negative integer. Number of recent drops used
as reference in the \code{probe()} drop magnitude check.
Default: \code{3L}.}

\item{drop_multiplier}{Positive numeric scalar. A new drop exceeding
this multiple of the recent median drop is flagged as a jump.
Default: \code{5.0}.}

\item{max_trials}{Optional positive integer. Maximum number of base
draws attempted by \code{sieve()} before stopping. \code{NULL}
defaults to \code{10 * R}. Default: \code{NULL}.}

\item{name}{Optional descriptive name.}

\item{...}{Additional metadata stored but unused internally.}
}
\value{
A \code{traversal_spec} object.
}
\description{
Specify the Branch Traversal Strategy
}
