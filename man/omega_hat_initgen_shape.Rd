% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omega-hat-strategies.R
\name{omega_hat_initgen_shape}
\alias{omega_hat_initgen_shape}
\title{Omega-Hat Initial-Guess Generator: Shape-Family Strategy}
\usage{
omega_hat_initgen_shape(
  param_mle,
  param_dim,
  psi_mle,
  param_lower = NULL,
  param_upper = NULL,
  p_shape = 0.7,
  shape_noise = 0.05,
  return_intent = TRUE,
  p_recenter = 0.1,
  p_far_recenter = 0.75,
  local_scale = 0.15,
  ...
)
}
\arguments{
\item{param_mle}{Numeric vector. MLE of the full parameter (logit space).}

\item{param_dim}{Integer. Length of param_mle (= J - 1).}

\item{psi_mle}{Numeric scalar. ψ(param_mle).}

\item{param_lower}{Optional numeric vector. Lower bounds.}

\item{param_upper}{Optional numeric vector. Upper bounds.}

\item{p_shape}{Numeric in (0,1]. Probability of shape-family path
vs Gaussian fallback.}

\item{shape_noise}{Nonnegative scalar. SD of jitter applied to the
shape-family probability vector before logit conversion. Breaks
exact within-family symmetry.}

\item{return_intent}{Logical. See \code{omega_hat_initgen_gaussian}.}

\item{p_recenter}{Numeric in [0,1). Probability of recentering.}

\item{p_far_recenter}{Numeric in (0,1]. Probability of choosing the
farthest history point when recentering.}

\item{local_scale}{Positive scalar. SD for Gaussian fallback.}

\item{...}{Unused. Absorbed for forward compatibility.}
}
\value{
A closure \code{function(history = NULL)} returning an intent
list or numeric vector.
}
\description{
Generates initial guesses from k-dominant canonical probability vectors
that analytically satisfy ψ(x0) ≈ ψ_mle. For each of the J categories,
a "k-dominant" vector assigns the dominant probability mass to category k
with the remainder shared equally, then jitters the result to break
exact symmetry.

This provides structurally diverse initial guesses that span different
basins of the constraint surface — complementing the Gaussian strategy,
which stays local to param_mle.

With probability \code{1 - p_shape}, falls back to a simple Gaussian
perturbation of param_mle.
}
\note{
Currently implemented for the Simpson's-index (sum-of-squares)
psi function. The k-dominant construction assumes psi_fn computes
D = sum(p_j^2) on the simplex. For other psi functions, the analytic
dominant-p derivation will not be exact — the sampler's feasibility
projection will correct small discrepancies.
}
